{{- $ckanEfs := print .Release.Name "-ckan-efs-pv-3" -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-check-links-efs-pv-3-pvc
spec:
  resources:
    requests:
      storage: {{ .Values.ckan.checkLinks.persistence.size | default "1Gi" }}
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $ckanEfs }}
      app.kubernetes.io/component: {{ $ckanEfs }}
