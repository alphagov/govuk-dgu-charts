apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .Release.Name }}-reindex-organisations
  labels:
    app.kubernetes.io/arch: {{ default "amd64" .Values.arch }}
spec:
  schedule: "0 8 * * *"
  jobTemplate:
    metadata:
      name: {{ .Release.Name }}-reindex-organisations
      labels:
        app.kubernetes.io/arch: {{ default "amd64" .Values.arch }}
    spec:
      template:
        metadata:
          name: {{ .Release.Name }}-reindex-organisations
          labels:
            app.kubernetes.io/arch: {{ default "amd64" .Values.arch }}
        spec:
          containers:
            - name: ckan
              image: '{{ include "docker-uri" (dict "environment" .Values.environment "app" "ckan" "files" $.Files) }}'
              imagePullPolicy: Always
              command: [ ckan, datagovuk, reindex-organisations ]
              env:
                {{- include "ckan.environment-variables" . | nindent 16 }}
              volumeMounts:
                - name: production-ini
                  mountPath: /config
                  readOnly: true
          restartPolicy: OnFailure
          volumes:
            - name: production-ini
              configMap:
                name: {{ .Release.Name }}-{{ .Values.ckan.ckanIniConfigMap }}
          {{- if eq "arm64" .Values.arch }}
          tolerations:
            - key: arch
              operator: Equal
              value: {{ .Values.arch }}
              effect: NoSchedule
          nodeSelector:
            kubernetes.io/arch: {{ .Values.arch }}
          {{- end }}