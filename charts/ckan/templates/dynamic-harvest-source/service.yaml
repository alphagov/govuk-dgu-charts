{{- if $.Values.dev.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dynamic-harvest-source
spec:
  selector:
    app: {{ .Release.Name }}-dynamic-harvest-source
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: http
{{- end }}