apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-datagovuk
spec:
  selector:
    app: {{ $.Release.Name }}-datagovuk
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-datagovuk-feature
spec:
  selector:
    app: {{ $.Release.Name }}-datagovuk-feature
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
