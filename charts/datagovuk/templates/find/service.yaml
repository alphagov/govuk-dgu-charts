{{- range $app := (list "find" "frontend-find") }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-{{ $app }}
spec:
  selector:
    app: {{ $.Release.Name }}-{{ $app }}
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http
---
{{- end }}
