name: Auto-Approve Image Tag Updates
on:
  pull_request:
    types: [opened]
  push:
    branches:
      - DGUK-506-automatically-merge-datagovuk-integration

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'pull_request' && contains(github.event.pull_request.title, 'Update datagovuk image tags for integration')) ||
      (github.event_name == 'push')
    
    permissions:
      pull-requests: write

    steps:
      - name: Approve the PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr review ${{ github.event.pull_request.number }} \
            --approve \
            --body "Automated approval for integration image tag update."